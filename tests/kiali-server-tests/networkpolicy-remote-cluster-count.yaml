name: "networkpolicy_remote_cluster_count"
description: "Verify no NetworkPolicy resources exist when remote_cluster_resources_only is true"
helm_args:
  - "--set"
  - "deployment.remote_cluster_resources_only=true"
yq_query: 'select(.kind == "NetworkPolicy") | .kind'
expected_result: ""
should_fail: false
