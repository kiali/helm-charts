name: "deployment_strategy_default"
description: "Verify the default deployment strategy"
helm_args: []
yq_query: "select(.kind == \"Deployment\") | .spec.strategy"
expected_result: |
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 1
  type: RollingUpdate
should_fail: false
