name: "deployment_priority_class"
description: "Verify priorityClassName configuration"
helm_args:
  - "--set"
  - "priorityClassName=high-priority"
yq_query: "select(.kind == \"Deployment\") | .spec.template.spec.priorityClassName"
expected_result: |
  high-priority
should_fail: false
