name: "skip_resources_serviceaccount"
description: "Verify ServiceAccount is not created but other resources are created when skipResources includes sa"
helm_args:
  - "--set"
  - "skipResources[0]=sa"
yq_query: ".kind"
expected_result: |
  ClusterRole
  ---
  ClusterRoleBinding
  ---
  Deployment
should_fail: false
